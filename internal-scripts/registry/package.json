{
  "name": "@skills-house/registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "skills-house-registry": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "validate": "tsc --noEmit",
    "test": "pnpm build && node --test test/registry.test.mjs"
  },
  "dependencies": {
    "@skills-house/build": "workspace:*",
    "ajv": "^8.17.1",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.0"
  }
}
