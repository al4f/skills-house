name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build compiler
        run: pnpm --filter @skills-house/build build

      - name: Build @skills-house/install
        run: pnpm --filter @skills-house/install build

      - name: Build @skills-house/create
        run: pnpm --filter @skills-house/create build

      - name: Build skills
        run: |
          for skill in skills/*/; do
            node internal-scripts/build/dist/cli.js "$skill"
          done

      - name: Test
        run: pnpm test

      - name: Pack build (smoke)
        run: node scripts/pack-build.mjs

      - name: Pack skill-auditor (smoke)
        run: node scripts/pack-skill.mjs skill-auditor

      - name: Pack install (smoke)
        run: node scripts/pack-install.mjs

      - name: Validate skill-auditor
        run: bash scripts/skill-auditor-tools/scripts/validate.sh skills/skill-auditor

      - name: Build registry generator
        run: pnpm --filter @skills-house/registry build

      - name: Generate and verify registry output
        run: pnpm generate:check
